name = "pythia"
compatibility_date = "2024-01-01"

# Cron trigger - runs daily at 3 AM UTC (FREE on Cloudflare)
[triggers]
crons = ["0 3 * * *"]

# This tells Cloudflare to call your cron function daily
# The function will be accessible at /cron/daily-scan
# You can also manually trigger it at: https://your-site.pages.dev/cron/daily-scan?action=scan
